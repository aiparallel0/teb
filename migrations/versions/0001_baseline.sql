-- Migration: baseline schema
-- Baseline marker — existing tables were created by init_db() in storage.py.
-- This migration records the starting point so future migrations apply cleanly.

-- No schema changes — this is a placeholder for the initial state.
SELECT 1;
